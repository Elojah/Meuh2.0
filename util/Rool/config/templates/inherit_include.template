#ifndef ${INC_GUARD}_H
# define ${INC_GUARD}_H

$[# include "${PARENT}.hpp"]

class ${CLASS_NAME} : $[${ACCESS} ${PARENT}]
{
public:
	${CLASS_NAME}(void);
	~${CLASS_NAME}(void);
protected:
private:
};

#endif
